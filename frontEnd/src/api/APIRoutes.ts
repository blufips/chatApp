const host = 'http://localhost:5000';
const loginAPI = `${host}/api/auth/login`;
const registerAPI = `${host}/api/auth/register`;
const setAvatarAPI = `${host}/api/auth/setAvatar`;

export { loginAPI, registerAPI, setAvatarAPI };
